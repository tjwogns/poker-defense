# 포커 디펜스: Royal Siege

포커 족보로 유닛을 뽑아 순환 경로를 도는 적을 막는 웹 디펜스 게임.
카드 조합, 군단 성장, 순환형 전장을 결합한 독립 오리지널 프로젝트다.

## v2.2 Life Economy Lab (public experiment)

- 공개 v2.1.2와 분리된 LAB 주소: https://tjwogns.github.io/poker-defense/lab/
- 로컬 개발 주소: `http://127.0.0.1:5173/?experiment=life`
- 라이프 20, 일반 적의 침투 5칸당 −1, 보스 탈출 시 즉시 −3
- 라운드당 무료 교환 3회, `날렵한 셔플` 등 교환 유물로 상한 증가
- 이자 5%·기본 상한 25G 실험, 기존 강화·덱 개조·유물 구매 골드 소비처 유지
- LAB는 공개 실험 중이며 밸런스 확정 전, 루트 클래식 모드는 변경하지 않음

## v2.1.2 Relic Choice

- 보스 유물 보상창에서 `이번 유물 보상 건너뛰기` 선택 가능
- 유물 5슬롯이 가득 차도 기존 빌드를 강제로 교체하지 않고 진행
- 빈 슬롯이 있어도 원하지 않는 유물 보상을 포기할 수 있음
- 대기 중인 보스 보상은 건너뛴 뒤 다음 선택창으로 이어서 처리

## v2.1.1 Windows Recovery

- Windows Chrome에서 알트탭 후 WebGL 컨텍스트가 사라질 때 자동 일시정지·복원
- 복구 중 흰 화면 대신 브라우저 DOM 기반 그래픽 복구 안내 표시
- 4초 안에 복구되지 않으면 Canvas 안정 모드로 다시 시작
- 사용자 입력 전 AudioContext 생성을 막고 실제 실행 상태에서만 효과음 재생
- 강제 WebGL 손실·자동 복구·Canvas 게임 진입 E2E 스모크 추가

## v2.1 Portrait Command

- 정식 게임 주소: https://tjwogns.github.io/poker-defense/
- 390×844·412×915급 휴대폰에서 회전 없이 세로 플레이 지원
- 세로 메뉴·전장·웨이브·손패·리롤 판단·대표 문양·배치·전투·패배 분석 UI
- 카드 66×92, 주요 행동 56px, 하단 유틸 50px의 터치 우선 조작
- 게임 규칙 좌표와 렌더링 좌표를 분리해 기존 전투 판정·밸런스 유지
- 360px 미만 또는 극단적으로 작은 가로 화면만 최소 크기 안내
- 데스크톱과 모바일 가로 모드는 기존 레이아웃 유지
- 보조 오버레이의 전용 세로 디자인은 후속 확장 예정

## v2.0 Deck Foundation

- 정식 게임 주소: https://tjwogns.github.io/poker-defense/
- 기존 `/poker-defense/v2/` 주소는 정식 게임 주소로 자동 이동
- 온라인 일일 랭킹과 동의 기반 익명 플레이 분석 연결
- 런 동안 유지되는 덱 원본과 40장 추방 하한·60장 복제 상한
- 복제된 동일 카드의 실제 사본 수를 반영하는 교환·정확 확률 계산
- 동일 카드를 여러 장 뽑아도 실제 런 덱 기준 확률 계산이 게임 진행을 중단하지 않도록 보정
- `D` 덱 뷰어에서 52종 카드별 보유량·현재 패 수량·추방/복제 가능 여부 확인
- 준비 단계 첫 교환 전만 허용되는 추방·복제 인장 명령과 오선택 안전장치
- 선택 카드 추방·복제 시 가장 크게 변하는 족보와 트리플+ 확률을 정확한 %p로 미리보기
- 10·20·30·40·50·60 보스전 직전 정비소에서 추방 인장 25G·복제 인장 40G 구매
- 구매 후 바로 덱 개조 화면으로 이동
- 일반/희귀/전설 60:30:10 등급 선추첨과 결정론적 보스·정비소 유물 진열
- 정비소 상품 3종: 추방 인장 25G·복제 인장 40G·미보유 유물 1개(45/90/160G)
- 유물 5슬롯, 등급별 판매(20/45/80G), 풀 슬롯에서 순비용을 확인하는 원자적 교체
- 조건부 신규 유물 8종과 유닛·배치·제어·덱 압축·족보 확정 기반 빌드
- 조건 충족 유물을 우측 BUILD 패널에서 점등하고, 라운드당 1회 익명 발동 이벤트로 측정
- 덱 개조·정비소 이벤트를 허용하고 분석 서버 CORS 응답을 보정
- 글자 토큰 대신 무기·체형으로 구분되는 전체 유닛 13종, 역할별 투사체·명중 이펙트, 공격 반동·처치 파티클·보스 등장/피격 연출
- 친근한 왕실 장난감 스타일의 견습병·궁수·쌍석궁병·화염술사·저격수·빙결술사 투명 캐릭터 스프라이트와 메뉴 마스코트
- 성기사·드래곤 기수·대마법사·신룡까지 표준 족보 10등급 전용 캐릭터 그래픽과 고등급 등장 강조
- 오중 군주·문장 기사단장·태초의 별까지 히든 족보 3등급 전용 초월 캐릭터와 강화된 등장 강조
- 철갑 딜러·혈월 여왕·시간 도둑·황금 폭군·군단왕·로열 조커 전용 보스 캐릭터와 광폭화 연출
- 보스 방어막·재생·가속 상태 표시와 골드 강탈·부하 소환 실시간 카운트다운·발동 예고
- 유물 18종 전용 투명 아이콘과 일반·희귀·전설 등급별 색·선 두께·전설 이중 프레임
- 정비소 족보 연마 Lv1~5와 해당 등급 기존·신규 유닛 전체 소급 피해 상승
- 승패 화면에서 광역·연쇄 포함 실제 피해 1위 족보·점유율·연마 레벨과 다음 빌드 추천 표시
- 마운틴·백스트레이트 특수 변형과 패의 대표 문양에 따른 스페이드·하트·다이아·클로버 패시브
- 대표 문양 `2-2-1` 동률 직접 선택, 유닛 문양 배지·투사체 색·동일 문양 합성 계승
- 모든 공격에 짧은 섬광·색상 링·방사형 피격 스파크를 추가하고 작은 터치 화면은 동시 이펙트 수를 자동 절감
- 둥근 버튼·눌림 반동·모바일 터치 안내와 핵심 조작 확대를 적용한 UI/UX 리뉴얼 1차
- Royal Siege 에디토리얼 다크 테마와 상단 전황 바·우측 명령 레일·하단 카드 도크 UI
- 리롤 판단 요약과 `전체 확률` 버튼을 분리해 겹침 없이 한눈에 확인
- 외부 웹폰트 없이 시스템 서체로 즉시 실행되는 안정적인 초기 로딩
- Windows Chrome의 중복·누락 포커스 이벤트에도 선택한 1·2·4배속을 보존하고 즉시 재개
- 패치 노트 내부 스크롤, 가로 휴대폰 화면 맞춤·터치 실행, `필드 적 n/80` 경고, 배치 ✓/× 패턴·추천 3칸
- 현재 HOLD 기준 `리롤 판단`·상향 확률·추천 행동·노림수와 첫 3라운드 상태 기반 플레이 코치
- Q/W/R/T 무늬 액티브를 제거하고 관련 유물 2종을 상시 패시브 효과로 전환
- 단순 유닛 계열 시너지를 전투 배율·도감·BUILD·패배 분석에서 제거하고 유물·족보 연마·대표 문양 빌드에 집중
- 복제 덱으로 발견하는 파이브 카드·플러시 하우스·플러시 파이브와 영구 잠금 해제 도감
- `D` 덱 뷰어의 히든 레시피 재료 진행도와 가장 가까운 복제 추천
- 정비소 진열 리롤은 유물 구매 데이터 측정 후 재검토

## 실행

```bash
npm install
npm run dev        # 개발 서버 (http://localhost:5173)
npm test           # 코어 로직 단위 테스트 (vitest)
npm run sim -- 30  # 헤드리스 밸런스 시뮬레이션 30판
npm run sim -- 30 compare  # 같은 시드로 정비소 4전략 경제 비교
npm run sim -- 30 relic-compare  # 미구매/무조건/선별 유물 구매 비교
npm run sim -- 5 clear           # 클리어 기준선: 즉시 강화·양쪽 인장·선별 유물·저족보 연마
npm run sim -- 5 clear-delay30   # 비교 실험: R1~30 강화 보류, R31부터 강화
npm run sim -- 5 clear-high6     # 고점 실험: 풀하우스3·포카드2·스트레이트플러시1 강제
npm run sim -- 30 hidden-compare  # 기본/복제/히든 추적 전략 비교
npm run sim -- 30 mastery-compare # 미구매/전부/저족보/고족보 연마 전략 비교
npm run build      # 타입체크 + 프로덕션 빌드 (dist/)
SINGLEFILE=1 npm run build  # 단일 HTML 파일 빌드 (배포/공유용)
node scripts/smoke.mjs      # E2E 스모크 (Chrome 필요, 프리뷰 서버 선행)
npm run smoke:hidden        # 히든 발견·도감 해금·저장 유지 E2E
npm run smoke:focus         # Chrome 탭 전환 후 선택 배속 유지 E2E
npm run smoke:recovery      # WebGL 강제 손실·자동 복구·Canvas 안정 모드 E2E
npm run smoke:mobile        # 가로·390×844 세로 모바일 실행과 터치 진입 E2E
npm run smoke:bosses        # 보스 6종 로딩·배치·최종 광폭화 비주얼 E2E
```

GitHub Actions 배포는 이전 해시 번들 삭제와 브라우저 HTML 캐시가 엇갈려 빈 화면이 생기지 않도록 자동으로 단일 HTML 빌드를 사용한다.

## 게임 규칙 요약

- 매 라운드 카드 5장 드로우 → 홀드 후 교환(무료 1회, 추가는 골드) → **족보 확정 = 그 등급의 유닛 획득** (하이카드도 최하급 유닛 지급)
- 패에서 가장 많은 문양이 유닛의 대표 문양이 되며 `2-2-1` 동률은 직접 선택. ♠ 보스 피해, ♥ 공격 속도, ♦ 처치 골드, ♣ 일반 적 피해를 강화
- `10-J-Q-K-A` 마운틴은 피해 +25%, `A-2-3-4-5` 백스트레이트는 공격 속도 +18% 변형을 얻음
- 유닛을 타일에 배치하면 자동 공격. 적은 죽을 때까지 사각 경로를 순환
- **필드의 적이 80마리를 넘으면 패배**. 10의 배수 라운드는 보스이며, 다음 라운드로 이월되더라도 처치하면 유물을 선택
- 60라운드는 스폰 완료 후 50초 안에 최종 보스를 처치해야 승리하며, 보스가 생존하면 패배
- 골드 사용처: 추가 교환(도박) vs 전역 공격력 강화(확정) vs 유닛 판매/재배치

| 족보 | 유닛 | 특성 |
|---|---|---|
| 하이카드 | 견습병 | 근거리 필러 |
| 원페어 | 궁수 | 기본 원거리 |
| 투페어 | 쌍석궁병 | 연사 |
| 트리플 | 화염술사 | 소형 스플래시 |
| 스트레이트 | 저격수 | 장거리 · 방어 무시 |
| 플러시 | 빙결술사 | 슬로우 |
| 풀하우스 | 성기사 | 아군 공격력 오라 |
| 포카드 | 드래곤 기수 | 대형 스플래시 |
| 스트레이트 플러시 | 대마법사 | 체인 라이트닝 |
| 로열 스트레이트 플러시 | 신룡 | 현재 HP 비례 피해 |
| 파이브 카드 | 오중 군주 | 5연쇄 공격 |
| 플러시 하우스 | 문장 기사단장 | 강력한 아군 오라 |
| 플러시 파이브 | 태초의 별 | 연쇄 공격 · 처형 |

## 구조

```
src/core/   순수 TS 게임 로직 — Phaser 의존성 0, 고정 틱(30/s) 결정론 시뮬레이션, 시드 RNG
            (cards/ 덱·족보판정, combat 전투, game 상태머신, balance 모든 수치)
src/game/   Phaser 3 렌더링/입력 — core 상태를 그리기만 한다
src/meta/   프로필·공유·동의 기반 익명 플레이 분석
src/sim/    헤드리스 밸런싱 시뮬레이터 (core만 import)
tests/      vitest 단위 테스트 (core 전체)
```

밸런스 수치는 전부 `src/core/balance.ts` 한 파일에서 관리한다.
밸런스 결과는 `npm run sim -- 100`으로 재현하며, 공개 테스트 데이터와 함께 릴리스마다 다시 기록한다.

## 익명 플레이 분석

- 최초 실행 시 사용자가 허용한 경우에만 이벤트를 기록한다.
- 메인 화면에서 분석 전송을 미시도·전송 중·브라우저 접수·서버 확인·실패로 구분해 확인할 수 있다.
- 기록 이벤트: 시작·튜토리얼·족보 확정·첫 전투·주요 라운드·보스 조우/처치/생존·무효 배치·합성·유물·종료·이탈·재도전·공유
- 이름, 이메일, 실제 카드 내용은 이벤트에 넣지 않는다.
- 같은 브라우저의 재방문 측정에는 로컬 무작위 방문 ID를 사용하고, 서버에는 단방향 해시만 저장한다.
- 기본 상태에서는 최근 500개 이벤트를 브라우저에만 저장하고 `LOG EXPORT`에 포함한다.
- 중앙 수집이 필요하면 HTTPS POST 수집 주소를 저장소의 GitHub Actions 변수 `VITE_ANALYTICS_ENDPOINT`로 설정한다. 요청 본문은 `{ schema: "poker-defense-event-v1", gameVersion, event }` 형식이다.
- 수집 주소는 CORS와 `sendBeacon` 요청을 허용해야 한다. 로컬 개발에서는 `.env.example`을 복사해 같은 변수를 설정할 수 있다.
- 공개 안내문은 `/privacy.html`에 포함된다.
- 운영 수집 API는 랭킹 Worker의 `/analytics`이며, 원본 이벤트는 최대 90일 보관한다. `cd leaderboard-worker && npm run analytics:summary`로 최근 7일 플레이·보스 퍼널과 D1 재방문율을 확인한다.

## 온라인 일일 랭킹 서버 연결

게임 UI는 `VITE_LEADERBOARD_ENDPOINT`가 비어 있어도 정상 실행되며, 이 경우 랭킹 화면에 서버 연결 전 상태가 표시된다. 실제 온라인 집계에는 `leaderboard-worker/`의 Worker와 D1 데이터베이스를 배포한다.

```bash
cd leaderboard-worker
npm install
npx wrangler login
npx wrangler d1 create royal-siege-leaderboard
# 출력된 database_id를 wrangler.jsonc에 입력
npm run db:remote
npx wrangler secret put PLAYER_HASH_SALT
npm run deploy
```

배포 후 GitHub 저장소의 Actions 변수 `VITE_LEADERBOARD_ENDPOINT`에 Worker HTTPS 주소를 저장하고 `Deploy to GitHub Pages` 워크플로를 다시 실행한다. 로컬 개발에서는 `.env`에 같은 변수를 넣는다.

## 저작권

게임의 자체 코드·문구·디자인은 별도 허가가 없는 한 모든 권리를 보유한다.
Phaser와 EventEmitter3 등 제3자 구성요소는 각각의 MIT 라이선스를 따른다.
배포본의 전체 고지는 [`public/THIRD_PARTY_NOTICES.txt`](public/THIRD_PARTY_NOTICES.txt)에서 확인할 수 있다.

## 로드맵 (기획안 12장)

- [x] M0~M2: 코어 루프, 유닛 10종, 적 6종, 60라운드, 경제, 시뮬레이터
- [x] M3: 사운드, 고족보 연출, 튜토리얼, localStorage 기록, 데일리 런
- [x] M4A: 유닛 합성, 유물 빌드, 점수·업적, 모바일 가로 모드 지원
- [x] M4B-1: 무늬 액티브, 고유 보스, 결과 공유, 플레이테스트 로그
- [ ] M4B-2: 무한 모드, 히든 레시피, 조커/상점
- [x] M4B-2A: 온라인 일일 TOP 10 랭킹 베타
