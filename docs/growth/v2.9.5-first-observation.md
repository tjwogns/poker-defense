# v2.9.5 배포 직후 첫 관측

- 조회 시각: 2026-09-08 15:13 UTC / 2026-09-09 00:13 KST.
- 공개 릴리스: v2.9.5, 커밋 `0d49377`. 시작 시 코드 작업 트리 깨끗함.
- 기존 운영 D1에 집계 SELECT 한 번만 실행했다. 쓰기·삭제·마이그레이션·요금제 변경·원시 ID 출력 없음.
- 실행: `npm run analytics:life -- --remote --version v2.9.5 --from 2026-09-08 --as-of 2026-09-09`.

## 관측 결과

LIFE 시작 코호트 0런, 튜토리얼 미완료 시작 0런, 단계별 도달 0건, 성숙 D1 분모 0. 비율·평균은 null이다. 이는 동의한 브라우저의 해당 버전 이벤트에서 시작이 관측되지 않았다는 뜻이지 실제 방문자/플레이어가 0명이라는 뜻이 아니다. 배포 직후이며 수집 동의·전송·필터·QA 혼입 한계가 있다. 이 결과로 재미·난이도·재방문 향상을 평가할 수 없다.

## 날짜 경계 발견

한국 날짜는 9월9일이지만 당시 UTC는 9월8일이었다. 지정 as-of는 미래 UTC 자정이므로 이 결과는 닫힌 하루 보고서가 아닌 **조회 시점까지의 부분 관측**이다. 해당 신규 버전은 분모 0이라 D1 수치를 오보고하지 않았지만 기존 도구는 미래 as-of가 들어가면 다른 코호트의 아직 진행 중인 재방문 날짜를 성숙한 것으로 볼 수 있다. 합성 SQLite 데이터로 재현 후 보완한다. 가짜 시간/이벤트를 운영 DB에 넣지 않는다.

수정: CLI 실행 직전 `asOf > 현재 UTC 날짜`이면 DB 호출 전에 `closed UTC dates only`로 거부한다. 날짜에 독립적인 SQL 생성기는 합성 fixture용으로 유지한다. 주입 시각으로 UTC 자정 직전/직후 및 한국 날짜 차이, 로컬·원격 가짜 실행기 미호출을 검증했다. 실제 CLI에 미래 날짜를 전달해 같은 안전 오류와 종료 코드 1도 확인했다(원격 DB 요청 없음). 전체 55파일474테스트·타입 검사·빌드·diff 검사 통과. 운영 집계 추가 조회/공개 게임 버전 변경/커밋/배포는 하지 않았다.

## 다음 판단

실제 신규 플레이어 관찰과 충분한 버전별 데이터가 필요하다. 봇이나 fixture로 인간 표본을 채우지 않는다. 외부 모집·개인정보 수집·비용 지출을 시작하지 않았다. 현재 단계에서 추가 난이도 변경의 근거는 없다.
