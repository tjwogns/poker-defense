# Poker Defense 작업 규칙

## 버전 릴리스와 문서

버전 번호를 올리거나 새 버전을 배포할 때는 코드만 변경하지 말고 아래 문서를 같은 작업에서 갱신한다.

1. `src/meta/patchNotes.ts`와 `README.md`에 사용자 관점의 변경사항을 기록한다.
2. 옵시디언 프로젝트 경로 `1. Project/20260825 포커 디펜스/` 아래에 해당 버전 폴더(`v1.x/`)를 만들거나 갱신한다.
3. 버전 폴더에는 최소한 릴리스 결과, 핵심 시스템 변경, 밸런스·테스트 결과, 배포·운영 상태를 남긴다.
4. 최상위 현행 문서 `00~09`의 규칙·수치·로드맵도 최신 상태로 동기화한다.
5. 이전 버전 폴더는 당시 기록으로 보존하고, 최신 규칙처럼 오해될 내용에는 후속 버전 링크를 덧붙인다.
6. 커밋·배포 완료 보고에 코드 버전, 테스트 결과, 공개 URL, 갱신한 옵시디언 문서를 함께 적는다.

옵시디언 문서 갱신까지 끝나지 않은 버전 업데이트는 완료로 간주하지 않는다.

## 멀티 에이전트 협업

규모가 있는 기능 개발, 밸런스 변경, 리팩터링, 릴리스 작업은 메인 에이전트가 총괄한다. 메인 에이전트는 요구사항과 최종 의사결정을 유지하고, 아래 프로젝트 전용 에이전트에 범위가 명확한 작업을 위임한다.

- `game_planner`: 플레이어 경험, 게임 규칙, 수용 기준과 범위 정의
- `data_analyst`: 현재 밸런스 수치, 시뮬레이션 결과와 변경 영향 분석
- `game_developer`: 승인된 계획에 따른 구현과 필요한 코드 테스트
- `game_tester`: 구현 후 독립 검증, 회귀·경계 조건·브라우저 동작 점검

### 기본 진행 순서

1. 메인 에이전트가 사용자 요청, 제약, 완료 조건을 정리한다.
2. 기획과 수치 판단이 모두 필요한 작업은 `game_planner`와 `data_analyst`를 병렬로 실행하고 두 결과를 기다린다.
3. 메인 에이전트가 상충하는 제안을 조정하고 구현 범위와 수용 기준을 확정한다.
4. 코드 변경은 원칙적으로 한 명의 `game_developer`가 맡는다. 여러 쓰기 에이전트가 같은 파일을 동시에 수정하지 않는다.
5. 구현이 끝난 뒤 `game_tester`가 독립적으로 테스트한다. 테스터는 제품 코드를 수정하지 않고 실패 증거와 재현 방법을 반환한다.
6. 실패가 발견되면 메인 에이전트가 수정 범위를 `game_developer`에게 전달하고, 수정 후 `game_tester`가 다시 확인한다.
7. 메인 에이전트는 변경 파일, 테스트 명령과 결과, 밸런스 영향, 남은 위험을 하나의 완료 보고로 통합한다.

단순 오탈자, 설명만 필요한 질문, 한 파일의 명백하고 작은 수정에는 불필요하게 하위 에이전트를 만들지 않는다. 서로 독립적인 조사·분석·검토에는 병렬 에이전트를 우선하고, 쓰기 작업은 충돌을 피하도록 순차적으로 진행한다.

### 공통 안전 규칙

- 작업 시작 시 `git status --short`를 확인하고 사용자의 기존 변경을 보존한다.
- 사용자 요청 없이 버전 번호 변경, 커밋, 푸시, 배포를 실행하지 않는다.
- 버전 변경이나 배포가 포함되면 위의 「버전 릴리스와 문서」 규칙을 완료 조건에 포함한다.
- 에이전트 간 인계에는 관련 파일, 결정 근거, 확인한 명령, 미해결 위험을 포함한다.
